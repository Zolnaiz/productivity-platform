services:
  backend:
    environment:
      NODE_ENV: development
      DB_SYNCHRONIZE: "true"
      DB_MIGRATIONS_RUN: "false"
      DB_LOGGING: "true"
      ENABLE_SWAGGER: "true"
      ALLOW_PUBLIC_OPERATIONS: "true"
